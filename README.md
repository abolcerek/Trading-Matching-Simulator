# Trading Matching Simulator

This project will implement a trading matching simulator. The frontend will use react while the backend will use go. It will use postgres to store user information and market data. RabbitMQ will act as the broker to all clients.